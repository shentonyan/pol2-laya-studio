# 复制为 .env 并填入你的 key。.env 已在 .gitignore 中，不会被提交。
OPENJEV_API_KEY=
# 可选：覆盖端点（默认 https://api.openjev.sh/v1）
# OPENJEV_BASE_URL=https://api.openjev.sh/v1
# 可选：Ollama 地址（默认 http://127.0.0.1:11434）
# OLLAMA_HOST=http://127.0.0.1:11434
